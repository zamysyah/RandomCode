// Buat array dengan nama buah yang berisi nama-nama buah
var buah = ["Pisang", "Durian", "Mangga"];

// Gunakan perulangan for untuk menampilkan setiap elemen dalam array buah
for (var i = 0; i < buah.length; i++) {
    // Tampilkan elemen saat ini menggunakan console.log()
    console.log(buah[i]);
}
